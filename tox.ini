[tox]
envlist = py39
skipsdist = True
 
[testenv]
deps =

    pytest
    pytest-cov
ignore_errors = True
commands =
    pytest --cov=progpy --cov-report term --cov-report xml:coverage.xml --verbose

[coverage:run]
relative_files = True
source = progpy/
branch = True